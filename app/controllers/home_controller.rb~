class HomeController < ApplicationController

  def index
    if !user_signed_in?
      redirect_to new_user_session_path
    end
  end
  def get_appraisees(id)
    appraiser = User.find(id)
    KraSheet.where(:appraiser_id =>appraiser.id, :)
  end
end

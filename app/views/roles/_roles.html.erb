<div class="row-fluid">
          <div class="span12">
              <h2 class="page-title">Roles <small>All roles listed.</small></h2>
          </div>
        </div>
          <div class="row-fluid" >
            <div class="span8">
              <div class="flash_notice" id='role_notice'><div class="alert alert-success"> <a class="close" data-dismiss="alert">×</a> <%= notice %> </div></div>
 <ul class="nav nav-pills nav-stacked">
<li class="active">
    <a href="#">
      <span class="badge "><%= Role.count %></span>
       Roles
    </a>
  </li>
</ul>
<% if Role.count > 0 %>
<table class="table table-hover">
  <tbody><tr> 
    <th>Roles</th>
  </tr>

  <% @roles.each do |role| %>
    <tr>
      <td><%= role.name %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<% end %>
            </div>
          </div>
      </div>

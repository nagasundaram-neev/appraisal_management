

<div class="flash_notice"><div class="alert alert-success"> <%= notice %> </div></div>
 
<ul class="nav nav-pills nav-stacked">
<li class="active">
    <a href="#">
      <span class="badge "><%= KraAttr.count %></span>
       KRA Attributes avaliable
    </a>
  </li>
</ul>
<% if KraAttr.count > 0 %>
<table class="table table-hover">
  <tbody><tr>
    <th>Title</th>
    <th>Descriptopm</th>
    <th></th>
    <th></th>
  </tr>

  <% @kra_attrs.each do |kra_attr| %>
    <tr>
      <td><%= kra_attr.name %></td>
      <td><%= kra_attr.desc %></td>
      <td><%= link_to "Edit", edit_kra_attr_path(kra_attr), :remote => true, class: "btn btn-warning" %></td>
      <td><%= link_to "Destroy", kra_attr, :remote => true, :confirm => 'Are you sure?', :method => :delete, class: "btn btn-danger"  %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<% end %>
